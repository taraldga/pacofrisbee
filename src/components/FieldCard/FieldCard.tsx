import * as React from 'react'

import { Link } from 'react-router-dom'

export interface HoleNavigationProps {
  fieldName: number;
  fieldImageUrl: number;
}

const HoleNavigation: React.FC<HoleNavigationProps> = ({
    fieldName,
    fieldImageUrl,
}) => {

  return (
    <div className="row center">
        
    </div>
  )
}

